/**
 * 基于layui封装的自定义JS模块入口文件
 * @author yulao
 */
layui.config({
	base : '/js/modules/'
})

//$(function() {
//	$(document).pjax('a', '#pjax-fragment',{fragment: '#pjax-fragment'});
//	$(document).on('pjax:send', function() {
//		NProgress.start();
//	})
//	$(document).on('pjax:complete', function() {
//		NProgress.done();
//	})
//});